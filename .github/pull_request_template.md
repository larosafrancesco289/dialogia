**What this changes**

**Why**

**Screenshots or a GIF** (anything visual)

---

- [ ] `scripts/ci.sh` passes
- [ ] Behaviour changes come with a test that fails without the fix
- [ ] Persisted data (`localStorage['dialogia-ui']`, IndexedDB `dialogia` and
      `dialogia-keys`) still loads, with a migration where needed
- [ ] ARCHITECTURE.md / DESIGN.md updated if a flow or the visual language changed
